"use client";
import {useEffect,useState} from "react";import {Activity,AlertTriangle,Check,Layers3,Radio} from "lucide-react";import {acknowledgeAlert,fetchAlerts,fetchSources} from "@/services/api";import {useTrackingStore} from "@/stores/tracking-store";import {useAuthStore} from "@/stores/auth-store";import type {DataSource,OperatorAlert} from "@/types/tracking";
export function LeftPanel(){const[sources,setSources]=useState<DataSource[]>([]);const visible=useTrackingStore(s=>s.visibleTypes),toggle=useTrackingStore(s=>s.toggleType),canManage=useAuthStore(s=>s.can("sources.manage"));useEffect(()=>{fetchSources().then(setSources).catch(()=>undefined)},[]);return <aside className="panel left"><h2><Radio/> DATA SOURCES {canManage&&<b className="access-badge">MANAGE</b>}</h2>{sources.map(s=><div className="source" key={s.id}><i className={s.status}/><span>{s.name}<small>{s.messages_per_minute} msg/min · {s.latency_ms??"—"} ms</small></span></div>)}<h2><Layers3/> OPERATIONAL LAYERS</h2>{["aircraft","drone","vehicle","vessel","unknown"].map(t=><label className="toggle" key={t}><input type="checkbox" checked={visible.has(t)} onChange={()=>toggle(t)}/><span>{t}</span></label>)}</aside>}
export function RightPanel(){const tracks=useTrackingStore(s=>s.tracks),id=useTrackingStore(s=>s.selectedId),t=id?tracks.get(id):undefined,[alerts,setAlerts]=useState<OperatorAlert[]>([]);useEffect(()=>{fetchAlerts().then(setAlerts).catch(()=>undefined)},[]);const acknowledge=async(alertId:string)=>{await acknowledgeAlert(alertId);setAlerts(current=>current.filter(alert=>alert.id!==alertId))};return <aside className="panel right"><h2><AlertTriangle/> ACTIVE INCIDENTS <b>{alerts.length}</b></h2><div className="incident-list">{alerts.length===0?<div className="empty">No active alerts</div>:alerts.map(alert=><article className={`incident ${alert.severity}`} key={alert.id}><span>{alert.severity}</span><strong>{alert.title}</strong><p>{alert.message}</p><small>{new Date(alert.created_at).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}</small><button onClick={()=>void acknowledge(alert.id)}><Check/>ACKNOWLEDGE</button></article>)}</div><h2><Activity/> SELECTED TARGET</h2>{t?<div className="details"><strong>{t.callsign??t.id}</strong><em>{t.classification} {t.type}</em><dl><dt>POSITION</dt><dd>{t.latitude.toFixed(5)}, {t.longitude.toFixed(5)}</dd><dt>ALTITUDE</dt><dd>{t.altitude?.toLocaleString()??"—"} ft</dd><dt>SPEED / HDG</dt><dd>{t.speed??"—"} kt / {t.heading?.toFixed(0)??"—"}°</dd><dt>VERT RATE</dt><dd>{t.vertical_rate??"—"} ft/min</dd><dt>CONFIDENCE</dt><dd>{Math.round(t.confidence*100)}%</dd><dt>STATUS</dt><dd className="ok">{t.status}</dd></dl></div>:<div className="empty">Select a target on the map</div>}</aside>}
