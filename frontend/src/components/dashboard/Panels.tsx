"use client";
import {useEffect,useState} from "react";import {Activity,AlertTriangle,Layers3,Radio} from "lucide-react";import {fetchSources} from "@/services/api";import {useTrackingStore} from "@/stores/tracking-store";import type {DataSource} from "@/types/tracking";
export function LeftPanel(){const [sources,setSources]=useState<DataSource[]>([]);const visible=useTrackingStore(s=>s.visibleTypes),toggle=useTrackingStore(s=>s.toggleType);useEffect(()=>{fetchSources().then(setSources).catch(()=>undefined)},[]);return <aside className="panel left"><h2><Radio/> DATA SOURCES</h2>{sources.map(s=><div className="source" key={s.id}><i className={s.status}/><span>{s.name}<small>{s.messages_per_minute} msg/min · {s.latency_ms??"—"} ms</small></span></div>)}<h2><Layers3/> OPERATIONAL LAYERS</h2>{["aircraft","drone","vehicle","vessel","unknown"].map(t=><label className="toggle" key={t}><input type="checkbox" checked={visible.has(t)} onChange={()=>toggle(t)}/><span>{t}</span></label>)}</aside>}
export function RightPanel(){const tracks=useTrackingStore(s=>s.tracks),id=useTrackingStore(s=>s.selectedId),t=id?tracks.get(id):undefined;return <aside className="panel right"><h2><AlertTriangle/> ACTIVE INCIDENTS <b>0</b></h2><div className="empty">No active alerts</div><h2><Activity/> SELECTED TARGET</h2>{t?<div className="details"><strong>{t.callsign??t.id}</strong><em>{t.classification} {t.type}</em><dl><dt>POSITION</dt><dd>{t.latitude.toFixed(5)}, {t.longitude.toFixed(5)}</dd><dt>ALTITUDE</dt><dd>{t.altitude?.toLocaleString()??"—"} ft</dd><dt>SPEED / HDG</dt><dd>{t.speed??"—"} kt / {t.heading?.toFixed(0)??"—"}°</dd><dt>VERT RATE</dt><dd>{t.vertical_rate??"—"} ft/min</dd><dt>CONFIDENCE</dt><dd>{Math.round(t.confidence*100)}%</dd><dt>STATUS</dt><dd className="ok">{t.status}</dd></dl></div>:<div className="empty">Select a target on the map</div>}</aside>}
